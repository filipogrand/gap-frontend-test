import { SearchCarPipe } from './search-car.pipe';

describe('SearchCarPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCarPipe();
    expect(pipe).toBeTruthy();
  });
});
